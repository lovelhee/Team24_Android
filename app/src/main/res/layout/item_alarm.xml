<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="alarm"
            type="com.challengeonair.challengeonairandroid.model.data.Alarm" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="8dp">

        <ImageView
            android:id="@+id/ivAlarm"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginHorizontal="10dp"
            android:background="@drawable/rounded_cover"
            android:scaleType="centerCrop"
            android:src="@{alarm.isConfirmed ? @drawable/ic_alarm_read : @drawable/ic_alarm_unread}"
            android:clipToOutline="true"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <TextView
            android:id="@+id/tvAlarm"
            android:layout_width="wrap_content"
            android:layout_height="50dp"
            android:layout_marginHorizontal="10dp"
            android:gravity="center"
            android:text="@{alarm.message}"
            tools:text="예약된 챌린지가 취소되었습니다."
            android:textSize="15sp"
            app:layout_constraintStart_toEndOf="@+id/ivAlarm"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <Button
            android:id="@+id/btnConfirm"
            android:layout_width="80dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@{alarm.isConfirmed ? @color/black : @color/main_red}"
            tools:text="@{alarm.isConfirmed ? @string/read_alarm : @string/unread_alarm}"
            android:background="@{alarm.isConfirmed ? @drawable/alarm_read_button : @drawable/alarm_unread_button}"
            android:textSize="12sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
